Location for testdata

- `config.e2e.yaml`: committed template config for the manual live end-to-end
	test in `tests/test_e2e_run_pipeline.py`. It uses a 1-month date window and
	placeholder credential values that are replaced from environment variables at
	test runtime.